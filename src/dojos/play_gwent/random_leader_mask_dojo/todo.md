# TODO — random leader mask dojo (placeholder)

Placeholder for a play_gwent dojo not built yet. Only an empty
`__init__.py` exists here.

- [ ] Decide what this dojo adds over `LeaderMaskedFromDeckDojo`
  (`../deck_card_mask_dojos.py`), which already masks the leader out of
  a deck and predicts it. The name suggests a variant of that task
  (e.g. masking a random card alongside, or instead of, the leader); pin
  the task down against `metrics/play_gwent/BRAINSTORM.md` before
  writing a metric for it.
- [ ] If it turns out to be the same task, delete this directory.
